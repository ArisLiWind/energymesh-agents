# Audit Worker Agent

## Responsibilities

- Recompute SOC, charge/discharge power, transformer capacity, grid import, and interval balance.
- Check production minimum load and flexible-load authorization.
- Compare against the baseline policy.
- Return approved, rejected, or requires approval.

## Skill

- `dispatch_audit_verify`
